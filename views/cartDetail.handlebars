<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle del Carrito</title>
    <link rel="stylesheet" href="/path/to/bootstrap.css">
</head>
<body>
    <div class="container">
        <h1 class="my-4">Detalles del Carrito</h1>
        {{#if cart}}
        <div class="row">
            <div class="col-12">
                <h2>Carrito ID: {{cart._id}}</h2>
                <ul class="list-group">
                    {{#each cart.products}}
                    <li class="list-group-item">
                        Producto ID: {{this.productId}} <br>
                        Cantidad: {{this.quantity}}<br>
                        producto: {{this.producId.title}}

                    </li>
                    {{/each}}
                </ul>
                <hr>
                <h2>Agregar Producto</h2>
                {{#each products}}
            </div>
        </div>
        {{else}}
        <p>No se encontraron detalles del carrito.</p>
        {{/if}}
    </div>
    <script src="/path/to/bootstrap.js"></script>
</body>
</html>
